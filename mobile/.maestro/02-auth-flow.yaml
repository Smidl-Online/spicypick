appId: ${APP_ID}
---
# Smoke test: verify auth screen elements are present
# Uses optional checks to tolerate different startup states across builds
- launchApp
- assertVisible:
    text: "Log In"
    optional: true
- tapOn:
    text: "Log In"
    optional: true
- assertVisible:
    text: "Email"
    optional: true
