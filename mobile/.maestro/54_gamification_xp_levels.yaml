appId: ${APP_ID}
---
- launchApp
- waitForAnimationToEnd

# Navigate to profile tab
- tapOn: "Profile"
- waitForAnimationToEnd

# Verify XP bar is visible with level info
- assertVisible:
    id: "profile-xp-bar"

- takeScreenshot: gamification-xp-levels
